// todo: make this a dynamic loader
// import config from "../../config";
// const loader = (theme="light") => {
//     import(`./${theme}/styles`).then(styles => {
//         return styles;
//         //console.log(styles);
//     });
// }
//
// export const light = loader("light");
// export const dark = loader("dark");
// export default loader(config.theme);

import styles from "./livvy/styles";

export default styles;
